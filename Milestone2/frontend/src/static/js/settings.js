import api from './APIClient'

// User Info to dynamically fill
const profilePic = document.querySelector('#previewImage');
const userName = document.querySelector('#name');
const userRole = document.querySelector('#userrole');
const userEmail = document.querySelector('#email');
const coachCode = document.querySelector('#coachcode');


// Buttons
const changePassButton = document.querySelector('#changepassword');
const logoutButton = document.querySelector('#logout');
const deleteAccButton = document.querySelector('#delete');

changePassButton.addEventListener('click', (e) => {
    // TODO
});

logoutButton.addEventListener('click', (e) => {
    // TODO
});

deleteAccButton.addEventListener('click', (e) => {
    // TODO
});